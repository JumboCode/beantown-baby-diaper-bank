# Example environment variables for the Beantown Baby Diaper Bank application
# Rename this file to `.env` and fill in the appropriate values before running the application.
DATABASE_URL="postgresql://{user}:{password}@{host}:{port}/{database}?schema=public"
DIRECT_URL="postgresql://{user}:{password}@{host}:{port}/{database}"