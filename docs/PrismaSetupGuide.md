# Prisma Setup Guide

1. Reach out to Cooper for database config file (I can't commit it to github because our repo is public)
2. Run `npm install`.
3. Run `npm run prisma:generate` to generate the prisma client
4. Run `npm run dev` to start the dev server
5. You should be good to go. Please reach out to Cooper if one of these steps doesn't work.
